现在你需要读取本目录的Stepper-ZiLiao内的所有串口驱动文档和其他有关资料，

1. 仔细读取库函数和main函数，总结这些文件的相同点和不同点，包括配置，库文件，main文件
2. 告知我他们的资源配置
3. 告诉我怎么进行跨芯片移植
4. 告诉我能实现哪些功能，能实现哪些场景，多多举例

+++

现在只关注串口方式，不再理会脉冲模式。

串口方式的各个示例代码，是不是都是只修改了main.c，而其他没有改变呢，也就是说我移植的时候（假设我DMA等硬件已经配置完成），我只需要移植X_V2库即可在main.c进行操作就行了，对吗

+++

我在思考怎么建立步进电机的控制系统，首先扫描Stepper-ZiLiao的资料，明确步进电机的功能，形成记忆，然后回答我：

1. 步进电机能不能记住自己的0点位置，即使掉电仍然记得
2. 步进电机每次都是串口发送信息，然后发回应答信息，那么我在高速控制的过程中，比如频繁设置电机的速度，同时又频繁的读取电机的位置，那么我怎么快速并且不出错的读取各自的应答消息呢
3. 步进电机的结构体应该怎么配置，目前我不考虑单串口控制多步进电机，而是双串口控制，那么怎么写结构体实现更好的管理呢，我目前只写了一点点，你给我提建议

+++

由于现在商家库函数是单个串口控制多个电机实现的驱动，我想改成单个串口控制单个电机，那么就需要修改库函数，主要是加入huart的选择等你给我最小修改建议

+++

我有两个电机，一个是直接旋转，一个是带动齿轮间接旋转（电机1），那么问题就是，float pulse_angle;需不需要加入齿轮比，我发现按下按键，电机1并不是旋转360度，而是多转了

+++

现在有新的问题，由于我在while里面和中断都在调用DAM发送，那么就可能产生冲突，在中断的DMA发送主要是读取相关信息，而在while里面的就是逻辑信息，中断的丢一个数据问题并不大，但是在while里面丢了就可能再也没有机会再设置，所以我想要一个能够while调用比成功，中断大概率成功DMA发送，并且两者 不会冲突的方案

+++

能否直接检测DMA发送完成，然后while里面的就直接while(DMA空闲可发)，毕竟最多等几ms ，在中断的就正常发

+++

重点在于数据发送不能太快，否则可能粘包，重点在于while里面的数据可能只触发一次，必须实现，那么怎么处理呢

+++

首先双串口是双驱动电机的，完全互不干扰

我打算实现只在5ms定时器发送信息，每次只发送一条，然后将整个步进电机指令封装成队列

队列空的时候进行位置or速度的查询，队列不空的时候，比如设置相对角度、配置速度、退出等，进行相应参数传递然后发送信息，你给我计划一下怎么实现这个队列计划调度

+++

能否实现以下场景：

1. 循环按键按下，停止电机1，必须能执行
2. 另一个中断要求控制电机的速度为200等参数
3. 两个电机以及对应的串口相互不干扰，你的代码能否做到

+++

步进电机距离水平画板（0.5*0.5m）1m，并且在中心：0.25m，步进电机到正方形的四个角点的各个参数你可以自拟，1号云台控制绕z轴旋转，2号控制激光笔上下运动，你写一个函数实现绘制本正方形（已知四个角点的各自的两个角度，你自拟，我主要是查看可行性，所以无所谓参数）

要求只能写代码在Mode_3里面，按下按键1开始运动，再次按下直接急停，先进行计划

+++





























