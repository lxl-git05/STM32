首先你需要扫描整个文件夹，知道这个工程的整体结构和硬件资源配置，我的要求是：

1. 首先只能修改Mymain.c和Encoder_Key.c / .h的内容
2. 暂时忽略掉AT24C02的内容，我后续要求你联动两个模块再说
3. 测试程序需要最简化更改
4. 现在帮我实现和 D:\github\2-2-STM32\STM32\Hardware\Encoder_Key\AAA-旋转编码器驱动参考必看\江协科技-参考程序 一样的效果即可

+++

我想要实现使用两个按键和一个旋转编码器（含有S1和S2以及下压Key）实现选定参数进行修改

我的核心构想（你也可以提出进行更好的建议）：

[步长 参数名称 参数值(整数 or 浮点数)]

然后旋转编码器顺时针旋转，参数+=或者-=步长 * 旋转计数值，立即生效，逆时针同理

但是我不知道怎么样实现多个参数的选择，同时程序还必须简洁清晰，以及步长的选择，以及参数的切换，目前我有两个按键和一个旋转编码器以及编码器自带的按键

+++

我要进行一些补充，首先Key_Check和Menu_Key.c已经废弃不再使用，其实我想要编码器和AT24C02联动起来，也就是修改后进行保存，也就是按下编码器按键可以保存当前参数，那么我决定步长提前规划好，后续不再修改步长，那么目前为止一个编码器和一个按键进行选择即可，单击向下选择，双击向上选择，

其实我的文件的后续业务结构都是如D:\github\2-2-STM32\STM32\Hardware\42-Stepper\Stepper_Driver的Mode系统一样，一个Mode_G + 若干个 Mode_x，我的想法是双击按键0实现Mode转换，那么每个Mode都有各自的按键系统，然后在各自系统里面各自有需要调整的参数，比如Mode2我来调节电机PID，Mode3我来调节MPU6050的参数，Mode1专门用来调节AT24C02的所有注册的参数，所以你需要帮助我



1. 首先移植D:\github\2-2-STM32\STM32\Hardware\42-Stepper\Stepper_Driver的Mode调用逻辑，主要是将Mymain变成Mode转移页面，然后新建Mode文件夹加入4个Mode，当前Mymain的文件可以转移到Mode2，Mode1如我上面说的作为专有参数调整页，这一步做完之后给我审核，然后进行下一步
2. 帮我思考怎么实现那么每个Mode都有各自的按键系统，然后在各自系统里面各自有需要调整的参数，比如Mode2我来调节电机PID，Mode3我来调节MPU6050的参数，Mode1专门用来调节AT24C02的所有注册的参数，进入各个Mode则可调参数指向第1个，然后按下按键1进行参数切换，旋转编码器更新当前参数(参数 += 编码器的步数 * 参数步长)按下编码器按键进行保存，并且存入AT24C02中

+++

我要进行一些补充：Key_Check和Menu_Key.c已经废弃不再使用，你觉得配置一个参数表，提前进行参数注册，步长写死，然后 ==旋转=改值，KEY1=切参数==方案如何
